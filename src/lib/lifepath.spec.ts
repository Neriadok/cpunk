import { getRandomEvent  } from'./lifepath';

describe('lifepath', () => {
  xit('should return an event', assertLifepath );

  function assertLifepath() {
    const given = '';
    const actual =  getRandomEvent();
    const expected = '';
    expect(actual).toBe(expected);
  }
});
