import { skills  } from'./skills';

describe('skills', () => {
  it('should do stuff', assertSkills );

  function assertSkills() {
    const given = '';
    const actual =  skills(given);
    const expected = '';
    expect(actual).toBe(expected);
  }
});
